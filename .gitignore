# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# FIFA Queue
env/*.json
!env/*.example.json
*.env
.env*
!.env.example

# Firebase (ainda não configurado)
google-services.json
GoogleService-Info.plist
firebase_options.dart
lib/firebase_options.dart
.firebase/

# Assinatura
*.jks
*.keystore
key.properties
*.p12
*.p8
*.mobileprovision
ios/Runner/GoogleService-Info.plist

# Supabase CLI (estado local/temporario; nunca versionar)
supabase/.temp/
supabase/.branches/

# Dataset de terceiro baixado a mao para o importer de cartas (Etapa 11) --
# nunca versionar arquivo de dados de outra licenca dentro do nosso repo.
tool/data/

# Catalogo real FC27 (Etapa 17B) -- o dump completo da EA (17k+ linhas) e
# baixado a mao pelo usuario, nunca por este agente (robots.txt da EA proibe
# coleta automatizada por IA). Nunca versionar o dump bruto; so os scripts,
# schemas e as fixtures pequenas e curadas (data/hall_of_fut_21.csv,
# legacy_samples/) ficam versionados.
docs/final_data/data/ea_raw/
docs/final_data/data/*.json
docs/final_data/output/

# Ferramentas locais de desenvolvimento -- nunca versionar
.claude/
.agents/
skills-lock.json
