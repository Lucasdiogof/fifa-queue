# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# FIFA Queue
env/*.json
!env/*.example.json
*.env
.env*
!.env.example

# Firebase (ainda não configurado)
google-services.json
GoogleService-Info.plist
firebase_options.dart
lib/firebase_options.dart
.firebase/

# Assinatura
*.jks
*.keystore
key.properties
*.p12
*.p8
*.mobileprovision
ios/Runner/GoogleService-Info.plist

# Supabase CLI (estado local/temporario; nunca versionar)
supabase/.temp/
supabase/.branches/

# Dataset de terceiro baixado a mao para o importer de cartas (Etapa 11) --
# nunca versionar arquivo de dados de outra licenca dentro do nosso repo.
tool/data/

# Catalogo real FC27 (Etapa 17B) -- o dump bruto vindo diretamente da EA
# continua fora do git (robots.txt da EA proibe coleta automatizada por IA,
# e o dump em si nunca foi obtido por este agente). Fixtures de terceiro
# que o usuario ja trouxe pro repo (Wrexist, WEFUT, Hall of FUT) o dono do
# produto autorizou versionar por inteiro (projeto de resenha, sem
# pretensao comercial) -- ficam fora desta lista de propósito.
docs/final_data/data/ea_raw/
docs/final_data/FC27_COMPLETE/
docs/final_data/output/

# Ferramentas locais de desenvolvimento -- nunca versionar
.claude/
.agents/
skills-lock.json

# FVM Version Cache
.fvm/